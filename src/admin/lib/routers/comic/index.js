"use strict";

module.exports = {

    register: function ( server ) {

        server.app.get( '/comic/:id', function ( request, response ) {
        } );

        server.app.post( '/comic', function ( request, response ) {
        } );

        server.app.post( '/comic/:id', function ( request, response ) {
        } );

    }

};
