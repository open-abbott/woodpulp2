"use strict";

module.exports = {

    register: function ( server ) {

        server.app.get( '/user/:id', function ( request, response ) {
        } );

        server.app.post( '/user', function ( request, response ) {
        } );

        server.app.post( '/user/:id', function ( request, response ) {
        } );

    }

};
