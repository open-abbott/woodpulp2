"use strict";

module.exports = {

    register: function ( server ) {

        server.app.get( '/strip/:id', function ( request, response ) {
        } );

        server.app.post( '/strip', function ( request, response ) {
        } );

        server.app.post( '/strip/:id', function ( request, response ) {
        } );

    }

};
